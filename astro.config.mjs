import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://zijundidu.github.io',  // 后面改成你的 GitHub Pages 地址
  // base: '/my-portfolio',  // 仓库名，如果是 username.github.io 则删除这行
});